import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "**",
      },
    ],
  },
  // Strict mode for production-quality code
  reactStrictMode: true,
};

export default nextConfig;
